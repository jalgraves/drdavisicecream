image:
  name: drdavisicecream
  repository: jalgraves/drdavisicecream
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

aws:
  regionCode:
istio:
  enabled: true
containerPort: 3034
replicaCount: 1
resources:
  requests:
    memory: 100Mi
  limits:
    memory: 1Gi

global:
  env:
